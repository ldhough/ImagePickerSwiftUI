import XCTest
@testable import SimpleSelector

final class SimpleSelectorTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(SimpleSelector().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
